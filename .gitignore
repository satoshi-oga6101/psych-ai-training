# OS / エディタ
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node
node_modules/
npm-debug.log*

# 生成物・ドラフト（コミットしない）
docs/data/exercises.draft.json
content/*/manifest.draft.json

# 配布用にzipで固めたもの（手作業で作る。中身は skill/ 配下にあるので二重管理しない）
*.zip

# 企画・下書き（public リポジトリなのでコミットしない。ローカルにのみ置く）
planning/
